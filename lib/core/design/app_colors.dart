import 'package:flutter/material.dart';

abstract final class TohyouColors {
  // Base
  static const background = Color(0xFF08090D);
  static const surface = Color(0xFF101218);
  static const surfaceElevated = Color(0xFF171923);
  static const surfaceHighlight = Color(0xFF202331);

  // Text
  static const textPrimary = Color(0xFFF4F4F7);
  static const textSecondary = Color(0xFFA5A8B5);
  static const textMuted = Color(0xFF686C7A);

  // Accent
  static const accent = Color(0xFFFF4FA3);
  static const accentSoft = Color(0xFFFF78B8);
  static const accentDeep = Color(0xFFC92F79);

  // Supporting accents
  static const cyan = Color(0xFF55D9FF);
  static const violet = Color(0xFF9B7CFF);

  // Semantic
  static const success = Color(0xFF55D68A);
  static const warning = Color(0xFFFFC857);
  static const error = Color(0xFFFF5C6C);
}